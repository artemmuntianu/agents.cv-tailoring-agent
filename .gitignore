.env
*.json
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
ENV/
artifacts/
scratch/
.idea/
.vscode/
model_state.json
*.pyc

# `*.json` above must not hide versioned, non-secret descriptors.
!charts/**/*.json
!deploy/**/*.json
!tests/**/*.json
!**/values.schema.json
!.github/**/*.json
!backoffice/**/*.json

# Backoffice (Astro): dependency and build output never enters git.
backoffice/node_modules/
backoffice/dist/
backoffice/.astro/

# Runtime data
queue/
heartbeat

# Helm: keep Chart.lock (pins upstream versions) but not the vendored packages -
# a stale .tgz silently shadows the local subchart source.
charts/*/charts/*.tgz

# Tooling caches
.ruff_cache/
.pytest_cache/
.mypy_cache/
